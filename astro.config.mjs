// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
// User site served at the domain root, so no `base` path is needed.
export default defineConfig({
  site: "https://stephennaboth.github.io",
});
